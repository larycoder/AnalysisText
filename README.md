Complier:
    For complier whole project:
        On foler src: (need have java)
            Run script: javac -cp :../bin:Source/json-simple-1.1.jar -d .. *.java

Run program:
    For init application:
        On folder AnalysisText:
            Run script: java -cp :bin:src/Source/json-simple-1.1.jar bin.MainRun